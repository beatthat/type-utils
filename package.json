{
  "name": "unity-type-utils",
  "version": "0.0.2",
  "description": "utils",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/beatthat/unity-type-utils.git"
  },
  "files": [
    "index.js",
    "scripts",
    "src"
  ],
  "author": "larrykirschner@gmail.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/beatthat/unity-type-utils/issues"
  },
  "homepage": "https://github.com/beatthat/unity-type-utils",
  "scripts": {
    "postinstall": "node scripts/postinstall.js",
    "install:test": "rm -f *.tgz && yarn cache clean && npm pack && cd test && npm run clean && yarn install"
  },
  "dependencies": {
    "ncp": "^2.0.0",
    "mkdirp": "^0.5.1"
  }
}
